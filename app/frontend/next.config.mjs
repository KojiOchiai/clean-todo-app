/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/static', // path prefix for the app
    assetPrefix: '/static', // path prefix for static files
};

export default nextConfig;
